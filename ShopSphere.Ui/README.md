ShopSphere.Ui

Minimal React app that queries the .NET API.

Quick start:

1. Set `REACT_APP_API_URL` to your API base URL (e.g. http://localhost:5000).
2. From this folder run:

```bash
npm install
npm start
```

This app shows a simple dashboard summary card grid and can be extended with charts and components described in the architecture.